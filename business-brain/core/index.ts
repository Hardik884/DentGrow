/**
 * Business Brain — Core barrel.
 *
 * The core exposes the foundational contracts that everything else depends
 * on. It must never depend on engines, services, or repositories.
 */
export * from "./engine";
