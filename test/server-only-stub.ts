/**
 * Stub for Next.js's `server-only` package under vitest.
 *
 * `server-only` has no resolvable entry point outside a Next build — it exists
 * so that importing a server module from a client bundle fails loudly. That
 * guard is worth keeping on the modules that carry it, so the import stays and
 * the test runner is pointed here instead of the import being deleted.
 */
export {};
